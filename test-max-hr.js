const fs = require('fs');

async function test() {
  // Can we fetch a local activity from the DB to see what 'max_heartrate' holds?
  // Let's just output my thought process.
  console.log("Checking if activity.max_heartrate is the activity max or the athlete's max...");
}

test();
